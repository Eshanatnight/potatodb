# target-cpu=native enables AVX-512 SIMD on AMD EPYC for vectorized Arrow
# kernels, hash operations, and memory copies. Disable for CI/portable builds.
[build]
rustflags = ["-C", "target-cpu=native"]

[env]
MACOSX_DEPLOYMENT_TARGET = "26.0"