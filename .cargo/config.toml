# For local max throughput on a known host, run with:
#   RUSTFLAGS="-C target-cpu=native" cargo build
# (AVX-512 on capable x86_64 hosts, etc.) Omit for CI/portable binaries.
[env]
MACOSX_DEPLOYMENT_TARGET = "26.0"